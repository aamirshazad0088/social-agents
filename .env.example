# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key



# Note: Copy this file to .env.local and fill in your actual values

# =============================================================================
# Python Backend Configuration
# =============================================================================

# Python backend URL (defaults to localhost:8000)
NEXT_PUBLIC_PYTHON_BACKEND_URL=http://localhost:8000

# API version
PYTHON_BACKEND_API_VERSION=v1

# Request timeout in milliseconds
PYTHON_BACKEND_TIMEOUT=30000

# Maximum retry attempts for failed requests
PYTHON_BACKEND_RETRY_ATTEMPTS=3

# Feature flags for gradual migration to Python backend
# Set to 'true' to enable Python backend for specific features
NEXT_PUBLIC_USE_PYTHON_BACKEND_CONTENT=true
NEXT_PUBLIC_USE_PYTHON_BACKEND_MEDIA=true
NEXT_PUBLIC_USE_PYTHON_BACKEND_SOCIAL=true
NEXT_PUBLIC_USE_PYTHON_BACKEND_STORAGE=true

# Master toggle: set to 'true' to use Python backend for ALL features
NEXT_PUBLIC_USE_PYTHON_BACKEND_ALL=true
